import React from 'react';
import Card from '../components/common/Card';

export default function Sensors() {
  return (
    <div>
      <h2 style={{ marginTop: 0, color: 'var(--color-text)' }}>Sensors</h2>
      <Card title="Sensor Grid">List and manage sensors.</Card>
    </div>
  );
}
