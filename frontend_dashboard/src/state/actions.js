//
// PUBLIC_INTERFACE
// actions.js
// Defines action type constants for a potential reducer-backed global store.
//

export const ACTIONS = Object.freeze({
  SET_PREFERENCE: 'SET_PREFERENCE',
  // Extend with domain actions if migrating from mocks to backend store
});
